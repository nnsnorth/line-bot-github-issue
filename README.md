# line-bot-github-issue
A LINE OA webhook server that receives support requests, classifies them using a multilingual ML model (k-NN + sentence embeddings), and automatically creates GitHub issues with project fields — severity, category, issue type, and SLA — pre-populated. Deployable to Google Cloud Run.
