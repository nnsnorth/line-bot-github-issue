# name: Refresh Docker Cache

# on:
#   schedule:
#     - cron: '0 2 * * 0'  # Every Sunday 02:00 UTC
#   workflow_dispatch:       # Allow manual trigger

# env:
#   WEBHOOK_IMAGE: ${{ vars.ARTIFACT_REGISTRY_IMAGE }}
#   CLASSIFIER_IMAGE: ${{ vars.CLASSIFIER_IMAGE }}

# jobs:
#   refresh-webhook:
#     runs-on: ubuntu-latest
#     environment: prod
#     permissions:
#       contents: read

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Authenticate to Google Cloud
#         uses: google-github-actions/auth@v2
#         with:
#           credentials_json: ${{ secrets.GCP_SA_KEY }}

#       - name: Configure Docker for Artifact Registry
#         run: gcloud auth configure-docker asia-southeast1-docker.pkg.dev --quiet

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Rebuild webhook image cache
#         uses: docker/build-push-action@v6
#         with:
#           context: .
#           push: false
#           no-cache: true
#           cache-to: type=registry,ref=${{ env.WEBHOOK_IMAGE }}:cache,mode=max

#   refresh-classifier:
#     runs-on: ubuntu-latest
#     environment: prod
#     permissions:
#       contents: read

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Authenticate to Google Cloud
#         uses: google-github-actions/auth@v2
#         with:
#           credentials_json: ${{ secrets.GCP_SA_KEY }}

#       - name: Configure Docker for Artifact Registry
#         run: gcloud auth configure-docker asia-southeast1-docker.pkg.dev --quiet

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Rebuild classifier image cache
#         uses: docker/build-push-action@v6
#         with:
#           context: classifier
#           push: false
#           no-cache: true
#           cache-to: type=registry,ref=${{ env.CLASSIFIER_IMAGE }}:cache,mode=max
